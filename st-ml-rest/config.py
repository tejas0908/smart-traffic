# this is path to faster rcnn pycaffe directory
faster_rcnn_path = '/home/ctolabs/softwares/py-faster-rcnn'  # '/path/to/faster/rcnn/'

# This is the path to rcnn trained model. It must contain
# 1. one caffemodel file. Ends with extension .caffemodel
# 2. one prototxt file. Ends with extension .prototxt
# 3. file which list classes in each line. Ends with extension .txt
rccn_model_path = '/home/ctolabs/projects/show-select/stree2shop'  # there should be caffemodel, deploy prototxt and
